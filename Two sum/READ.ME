2 solutions are given.

first one is in O(n^2), where we use brute force approach.
second one is using the concept of hashmap (in python we can use dictionaries to implement this). Searching for an element in dictionary is O(constant).
